import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

// RELATED
import { FooterComponent } from './footer.component';

// IMPORT MODULES, DECLARE/EXPORT COMPONENTS
@NgModule({
  imports: [
  ],
  declarations: [
    FooterComponent
  ],
  exports: [
    FooterComponent
  ]
})
export class FooterModule { }
